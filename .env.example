MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

CLICKHOUSE_USER=analytics
CLICKHOUSE_PASSWORD=analytics_pass
CLICKHOUSE_DB=analytics
# Airflow runs as root in compose so docker.sock works on Docker Desktop.
# Keep this for reference if you switch back to a non-root user on Linux.
AIRFLOW_UID=0